interface:
  display_name: "原型 → 后端需求说明"
  short_description: "基于原型 PRD 先做冲突 Q&A，再生成后端需求说明"
  default_prompt: "用于 hetu-cloud：读取原始原型/PRD（txt/md），先生成带编号问题的《冲突说明（Q&A）》供我逐条回答，再基于“原文 + 回答”生成偏后端的《需求说明》（数据模型、枚举、校验规则、状态流转、接口清单）。进入第 2 步前，先检查 Q&A 是否存在空回答（可追问或统一标注 TBD），并输出一段 Decision Log 汇总 Q1/Q2/... 的最终结论。"
